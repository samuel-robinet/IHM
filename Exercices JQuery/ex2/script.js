// TODO (1) Definissez la fonction cacherSpoilers() qui caches tous les paragraphes du document HTML
// - pour recuperer tout les elements de la meme balise on utilise $("balise"), par exemple, $("p") pour recuperer tous les paragraphs
// - pour cacher un element ou plusieurs elements on utilise la fonction .hide(), par exemple:
// * cacher un paragraphe avec l'id "monP": $("#monP").hide()
// * cacher tous les paragraphes: $("p").hide()

// TODO (3) Definissez la fonction afficherSpoilers() qui affiche tous les paragraphes du document HTML
// - pour afficher un element ou plusieurs elements on utilise la fonction .show(), par exemple: 
// * afficher un paragraphe avec l'id "monP": $("#monP").show() 
// * afficher tous les paragraphes: $("p").show()

